<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.softfinger.seunghyun.daechilife">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TimeTableFragment.AddClassActivity"
            android:windowSoftInputMode="stateHidden"
            android:theme="@style/Theme.AppCompat.Translucent" ></activity>
        <activity android:name=".TimeTableFragment.AddClassTimeTableActivity"
            android:theme="@style/noAnimTheme"></activity>
        <activity android:name=".BoardFragment.board"></activity>
        <activity android:name=".BoardFragment.articledetail"></activity>
        <activity android:name=".BoardFragment.board_newarticle"></activity>
        <activity android:name=".MyFragment.mypage_setting"></activity>
    </application>

</manifest>